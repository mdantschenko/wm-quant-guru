[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "wmguru"
version = "0.1.0"
requires-python = ">= 3.11"
dependencies = [
    "scipy",
    "numpy",
    "pytest"
]

[tool.setuptools.packages.find]
where = ["src"]