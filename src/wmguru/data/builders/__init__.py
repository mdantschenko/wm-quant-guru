"""Builders that derive a feature out of data already on disk or from events.

A builder never invents a number, it aggregates one: ratings out of results,
squad values out of player valuations, style out of event streams.
"""
