"""Everything that brings outside data in and puts it into a usable shape.

downloads fetch whole datasets, fetchers ask an endpoint per item, extractors
pull a part out of a dataset that is already on disk, builders derive features,
and compute works out numbers that no source carries.
"""
