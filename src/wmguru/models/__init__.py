"""Layer 1 and 2 of the concept: from features to a scoreline distribution.

MatchPredictionModel is the abstract base every model implements, so a new
model is a new subclass and never a change to an existing one.
"""
